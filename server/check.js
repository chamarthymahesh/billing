const mongoose = require('mongoose'); require('dotenv').config(); const Invoice = require('./models/Invoice'); const fs = require('fs'); mongoose.connect(process.env.MONGO_URI).then(async () => { const inv = await Invoice.findOne({ invoiceNumber: 'INV/26-27/001' }).lean(); fs.writeFileSync('inv_out.json', JSON.stringify(inv, null, 2)); mongoose.disconnect(); }).catch(console.error);
